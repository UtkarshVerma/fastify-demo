{
	"name": "fastify-demo",
	"version": "1.0.0",
	"description": "",
	"main": "dist/index.js",
	"type": "module",
	"scripts": {
		"prestart": "npm run build",
		"build": "tsc",
		"start": "node .",
		"dev": "concurrently 'tsc --watch' 'nodemon -q -w dist .'",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"concurrently": "^8.0.1",
		"nodemon": "^2.0.22",
		"typescript": "^5.0.4"
	},
	"dependencies": {
		"@fastify/rate-limit": "^8.0.0",
		"fastify": "^4.17.0"
	}
}
